declare module "input" {
  // Define the module's methods and properties using TypeScript syntax
  export function text(prompt: string, options?: any): Promise<string>;
  // Add other methods you use from the 'input' module
}
